import { useEffect, useMemo, useState } from 'react';
import axios from 'axios';
import { BrowserRouter, useLocation, useNavigate } from 'react-router-dom';
import { AreaChart, Area, BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, PieChart, Pie, Cell } from 'recharts';
import { LayoutDashboard, Wallet, FileText, TrendingUp, Target, Activity, BookOpen, Sparkles, ShieldCheck, Bell, ChevronRight, Upload, Plus, ArrowUpRight, ArrowDownRight, Send, Check, LockKeyhole, Trash2, CircleHelp } from 'lucide-react';
import '@/App.css';

const API = `${process.env.REACT_APP_BACKEND_URL}/api`;
const nav = [{label:'Dashboard',path:'/',icon:LayoutDashboard},{label:'My Finances',path:'/finances',icon:Wallet},{label:'Statements',path:'/statements',icon:FileText},{label:'Six-Month Analysis',path:'/analysis',icon:TrendingUp},{label:'Goals & Priorities',path:'/goals',icon:Target},{label:'Financial Changes',path:'/changes',icon:Activity},{label:'Finaura Learn',path:'/learn',icon:BookOpen},{label:'Ask Finaura',path:'/ask',icon:Sparkles},{label:'Settings & Privacy',path:'/settings',icon:ShieldCheck}];
const money = (n) => `₹${Number(n).toLocaleString('en-IN')}`;
const pct = (a,b) => Math.round((a/b)*100);

function Shell({children}) { const location=useLocation(); const navigate=useNavigate(); const active=nav.find(n=>n.path===location.pathname)||nav[0]; return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">f</span><span>finaura</span></div><div className="demo-pill" data-testid="demo-data-label"><span></span> Demo data</div><nav>{nav.map(({label,path,icon:Icon})=><button data-testid={`nav-${label.toLowerCase().replaceAll(' ','-').replaceAll('&','and')}`} className={active.path===path?'nav-item active':'nav-item'} onClick={()=>navigate(path)} key={path}><Icon size={18}/><span>{label}</span>{active.path===path&&<ChevronRight size={14} className="nav-arrow"/>}</button>)}</nav><div className="sidebar-bottom"><div className="privacy-mini"><LockKeyhole size={16}/><div><strong>Your data, yours.</strong><small>Prototype privacy controls active</small></div></div><div className="profile"><div className="avatar">AS</div><div><strong>Aarav Sharma</strong><small>Demo profile</small></div><CircleHelp size={17}/></div></div></aside><main className="main"><header className="topbar"><div><span className="eyebrow">{active.label}</span><h1 data-testid="page-title">{active.label}</h1></div><div className="top-actions"><div className="status"><span></span> Updated just now</div><button data-testid="notifications-button" className="icon-btn"><Bell size={19}/></button><div className="avatar small">AS</div></div></header>{children}</main></div> }

function Card({children,className=''}){return <section className={`card ${className}`}>{children}</section>}
function SectionTitle({eyebrow,title,action}){return <div className="section-title"><div><div className="eyebrow">{eyebrow}</div><h2>{title}</h2></div>{action}</div>}

function Dashboard({data}) { const s=data.summary; return <><div className="welcome"><div><div className="eyebrow">Friday, 28 August 2026</div><h2>Good morning, Aarav<span className="mint">.</span></h2><p>Here’s what’s happening with your financial life.</p></div><button data-testid="dashboard-add-data-button" className="primary-btn" onClick={()=>window.location.href='/statements'}><Plus size={17}/> Add financial data</button></div><div className="metric-grid">{[['Monthly income',s.income,'positive','vs. last month'],['Monthly expenses',s.expenses,'negative','vs. last month'],['Monthly savings',s.savings,'positive','34% savings rate'],['Estimated net worth',s.net_worth,'neutral','across all accounts']].map(([label,value,tone,sub],i)=><Card key={label} className="metric-card"><div className="metric-top"><span>{label}</span><span className={`metric-icon ${tone}`}>{i===1?<ArrowDownRight size={17}/>:<ArrowUpRight size={17}/>}</span></div><strong data-testid={`metric-${i}`}>{money(value)}</strong><small className={tone==='negative'?'down':''}>{sub}</small></Card>)}</div><div className="dashboard-grid"><Card className="trend-card"><SectionTitle eyebrow="Cash flow" title="Your financial rhythm" action={<span className="legend"><i></i> Income <b></b> Expenses</span>}/><ResponsiveContainer width="100%" height={235}><AreaChart data={data.history}><defs><linearGradient id="income" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#10b981" stopOpacity=".25"/><stop offset="100%" stopColor="#10b981" stopOpacity="0"/></linearGradient></defs><CartesianGrid stroke="#edf1ef" vertical={false}/><XAxis dataKey="month" axisLine={false} tickLine={false} tick={{fill:'#94a3b8',fontSize:12}}/><YAxis hide/><Tooltip formatter={(v)=>money(v)} contentStyle={{border:'1px solid #e2e8f0',borderRadius:8}}/><Area type="monotone" dataKey="income" stroke="#10b981" fill="url(#income)" strokeWidth={2.5}/><Area type="monotone" dataKey="expenses" stroke="#f59e0b" fill="none" strokeWidth={2.5} strokeDasharray="5 5"/></AreaChart></ResponsiveContainer></Card><Card className="health-card"><SectionTitle eyebrow="Financial health" title="A clear picture"/><div className="health-score"><div className="score-ring"><strong data-testid="health-score">78</strong><span>/ 100</span></div><div><strong>Looking good</strong><p>Your savings habits are doing the heavy lifting.</p></div></div><div className="health-bars">{[['Savings',88],['Spending',72],['Debt',69],['Goal progress',76]].map(([x,v])=><div key={x}><div><span>{x}</span><b>{v}</b></div><div className="bar"><i style={{width:`${v}%`}}></i></div></div>)}</div></Card></div><div className="dashboard-grid lower"><Card><SectionTitle eyebrow="Goals" title="Your north stars" action={<button data-testid="view-goals-button" className="text-btn" onClick={()=>window.location.href='/goals'}>View all <ChevronRight size={15}/></button>}/>{data.goals.slice(0,3).map(g=><GoalRow goal={g} key={g.id}/>)}</Card><Card><SectionTitle eyebrow="Finaura insight" title="Worth noticing"/><div className="insight"><div className="insight-mark">✦</div><div><strong>Your savings are back on track</strong><p>After dipping in June, you saved <b>₹62,000</b> this month — a 11% increase from your recent average.</p><button data-testid="insight-details-button" className="text-btn">See the details <ChevronRight size={15}/></button></div></div></Card></div></> }
function GoalRow({goal}){return <div className="goal-row"><span className="goal-emoji">{goal.emoji}</span><div className="goal-info"><div><strong>{goal.name}</strong><span className={`priority ${goal.priority.toLowerCase()}`}>{goal.priority}</span></div><div className="goal-progress"><span style={{width:`${pct(goal.current_amount,goal.target_amount)}%`}}></span></div><small>{money(goal.current_amount)} of {money(goal.target_amount)} <b>{pct(goal.current_amount,goal.target_amount)}%</b></small></div></div>}

function Finances({data}){return <><div className="page-intro"><div><div className="eyebrow">The full picture</div><h2>My finances</h2><p>Everything you’ve told Finaura, organized in one place.</p></div><span className="data-note"><LockKeyhole size={15}/> Private to your profile</span></div><div className="finance-grid">{[['Cash savings',data.summary.current_savings,'Liquid reserves'],['Investments',data.summary.investments,'Long-term growth'],['Loans & debt',data.summary.debt,'Outstanding principal'],['Monthly EMI',data.summary.emi,'Fixed obligation']].map(([a,b,c])=><Card className="finance-stat" key={a}><small>{a}</small><strong>{money(b)}</strong><span>{c}</span></Card>)}</div><Card><SectionTitle eyebrow="Monthly allocation" title="Where your money goes"/><div className="allocation"><ResponsiveContainer width="46%" height={230}><PieChart><Pie data={data.spending} dataKey="value" innerRadius={70} outerRadius={95} paddingAngle={3}>{data.spending.map(x=><Cell key={x.name} fill={x.color}/>)}</Pie><Tooltip formatter={(v)=>money(v)}/></PieChart></ResponsiveContainer><div className="allocation-list">{data.spending.map(x=><div className="allocation-line" key={x.name}><i style={{background:x.color}}></i><span>{x.name}</span><b>{money(x.value)}</b><small>{Math.round(x.value/123000*100)}%</small></div>)}</div></div></Card></>}

function Statements({data}){const [uploaded,setUploaded]=useState(false); const [txns,setTxns]=useState(data.transactions); const update=async(id,category)=>{setTxns(txns.map(t=>t.id===id?{...t,category}:t)); await axios.patch(`${API}/transactions/${id}`,{category});}; return <><div className="page-intro"><div><div className="eyebrow">Enter · Understand · Organize</div><h2>Statements</h2><p>No bank account connection required. Bring your history, your way.</p></div><button data-testid="upload-statement-button" className="primary-btn" onClick={()=>setUploaded(true)}><Upload size={17}/> Upload statement</button></div>{uploaded&&<div className="success-banner" data-testid="statement-upload-success"><Check size={18}/> Demo statement imported — review the categorization before confirming.</div>}<div className="upload-grid"><Card className="upload-card"><div className="upload-icon"><Upload size={22}/></div><h3>Drop a statement here</h3><p>PDF, CSV or Excel · up to 10 MB</p><button data-testid="simulate-upload-button" className="outline-btn" onClick={()=>setUploaded(true)}>Use demo six-month data</button></Card><Card><SectionTitle eyebrow="How Finaura works" title="You stay in control"/>{[['01','Import','Bring in a file or enter a transaction manually.'],['02','Review','Finaura suggests a category — never silently.'],['03','Understand','See your patterns across six months.']].map(x=><div className="step" key={x[0]}><span>{x[0]}</span><div><strong>{x[1]}</strong><p>{x[2]}</p></div></div>)}</Card></div><Card><SectionTitle eyebrow="August 2026 · 6 transactions" title="Review your imported data" action={<span className="review-pill"><span></span> Needs your review</span>}/><div className="table-wrap"><table><thead><tr><th>Date</th><th>Description</th><th>Type</th><th>Category</th><th>Amount</th></tr></thead><tbody>{txns.map(t=><tr key={t.id}><td>{t.date}</td><td><strong>{t.description}</strong></td><td><span className={t.type==='Income'?'income-tag':'expense-tag'}>{t.type}</span></td><td><select data-testid={`transaction-category-${t.id}`} value={t.category} onChange={e=>update(t.id,e.target.value)}>{['Income','Food','Shopping','Transport','Rent','Bills','Education','Entertainment','Healthcare','Other'].map(c=><option key={c}>{c}</option>)}</select></td><td className={t.type==='Income'?'amount-income':''}>{t.type==='Income'?'+':'−'}{money(t.amount)}</td></tr>)}</tbody></table></div></Card></>}

function Analysis({data}){return <><div className="page-intro"><div><div className="eyebrow">Patterns over time</div><h2>Six-month analysis</h2><p>Small shifts become clear when you step back.</p></div><span className="period-selector">Mar — Aug 2026 ▾</span></div><Card className="analysis-chart"><SectionTitle eyebrow="Income vs expenses" title="The gap is your resilience"/><ResponsiveContainer width="100%" height={270}><BarChart data={data.history} barGap={8}><CartesianGrid stroke="#edf1ef" vertical={false}/><XAxis dataKey="month" axisLine={false} tickLine={false}/><YAxis hide/><Tooltip formatter={(v)=>money(v)}/><Bar dataKey="income" fill="#10b981" radius={[4,4,0,0]}/><Bar dataKey="expenses" fill="#e2e8f0" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></Card><Card><SectionTitle eyebrow="Monthly breakdown" title="Your six-month record"/><div className="table-wrap"><table><thead><tr><th>Month</th><th>Income</th><th>Expenses</th><th>Savings</th><th>Savings rate</th></tr></thead><tbody>{data.history.map(m=><tr key={m.month}><td><strong>{m.month} 2026</strong></td><td>{money(m.income)}</td><td>{money(m.expenses)}</td><td className="amount-income">{money(m.savings)}</td><td><span className="rate-chip">{m.savings_rate}%</span></td></tr>)}</tbody></table></div></Card></>}

function Goals({data}){const [goals,setGoals]=useState(data.goals); const [show,setShow]=useState(false); const [name,setName]=useState(''); const [error,setError]=useState(''); const add=async()=>{if(!name)return; const g={name,target_amount:500000,current_amount:0,deadline:'2030',priority:'Medium',monthly_contribution:10000}; try{const r=await axios.post(`${API}/goals`,g); setGoals([...goals,{...r.data,emoji:'✦'}]);setName('');setError('');setShow(false)}catch(e){setError('We couldn’t save that goal just now. Please try again.')}}; return <><div className="page-intro"><div><div className="eyebrow">Prioritize what matters</div><h2>Goals & priorities</h2><p>Give every rupee a purpose, then choose what comes first.</p></div><button data-testid="create-goal-button" className="primary-btn" onClick={()=>setShow(true)}><Plus size={17}/> New goal</button></div><div className="goals-grid">{goals.map(g=><Card className={`goal-card ${g.priority.toLowerCase()}`} key={g.id}><div className="goal-card-top"><span className="goal-emoji large">{g.emoji||'✦'}</span><span className={`priority ${g.priority.toLowerCase()}`}>{g.priority} priority</span></div><h3>{g.name}</h3><div className="goal-big-amount">{money(g.current_amount)} <small>/ {money(g.target_amount)}</small></div><div className="goal-progress thick"><span style={{width:`${pct(g.current_amount,g.target_amount)}%`}}></span></div><div className="goal-meta"><span>{pct(g.current_amount,g.target_amount)}% complete</span><span>Due {g.deadline}</span></div><div className="goal-requirement"><span>Monthly contribution</span><b>{money(g.monthly_contribution)} / mo</b></div></Card>)}</div>{show&&<div className="modal-backdrop"><div className="modal"><button data-testid="close-goal-modal-button" className="modal-close" onClick={()=>setShow(false)}>×</button><div className="eyebrow">New goal</div><h3>What are you working toward?</h3><input data-testid="goal-name-input" value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. New home, sabbatical"/><button data-testid="save-goal-button" className="primary-btn full" onClick={add}>Save goal</button>{error&&<div className="error-message" data-testid="goal-save-error">{error}</div>}</div></div>}</>}

function Changes({data}){return <><div className="page-intro"><div><div className="eyebrow">What changed?</div><h2>Financial changes</h2><p>Meaningful signals from your financial life, not just numbers.</p></div><span className="signal-summary"><i></i> 2 positive · 1 to watch</span></div><div className="alert-card"><div className="alert-symbol">↘</div><div><span className="eyebrow">Savings alert · Significant</span><h3>Your savings dipped, then recovered</h3><p>Monthly savings decreased by approximately ₹17,000 between April and June, before recovering to ₹62,000 in August.</p></div><button data-testid="change-insight-button" className="text-btn">Explore signal <ChevronRight size={15}/></button></div><Card className="timeline-card"><SectionTitle eyebrow="Your story · March to August" title="The moments that shaped your money"/><div className="timeline">{[['MAR','Stable financial condition','Savings rate held at 41%','positive'],['APR','Food spending increased','Discretionary spending +9%','moderate'],['MAY','Income increased','Salary moved up by ₹5,000','positive'],['JUN','Savings rate decreased','Expenses reached ₹1,26,000','negative'],['JUL','Shopping became a pattern','Shopping up 18% from average','negative'],['AUG','Back on track','Savings recovered to ₹62,000','positive']].map((m,i)=><div className={`timeline-item ${i===5?'current':''}`} key={m[0]}><div className={`timeline-dot ${m[3]}`}></div><div className="timeline-date">{m[0]}</div><div><strong>{m[1]}</strong><p>{m[2]}</p></div>{i<5&&<div className="timeline-line"/>}</div>)}</div></Card></>}

function Learn(){const cards=[['Emergency funds','Personal finance','Because your current cash reserve is below 3 months of expenses.','8 min read','mint'],['Mutual funds 101','Investing basics','You have a long-term goal but no investments recorded yet.','6 min read','dark'],['The power of compounding','Long-term planning','A small monthly habit can become meaningful over time.','5 min read','yellow'],['Making sense of inflation','Personal finance','Understand why your goals need a little headroom.','7 min read','peach']]; return <><div className="page-intro"><div><div className="eyebrow">Knowledge, made relevant</div><h2>Finaura Learn</h2><p>Financial education shaped around your goals — never a generic feed.</p></div><span className="learn-count">4 recommendations for you</span></div><div className="learn-feature"><div><div className="eyebrow">Recommended for you · 01</div><h2>Build your safety net first</h2><p>Your emergency fund is the foundation beneath every other goal. Learn how to choose a number that feels right for your life.</p><button data-testid="learn-feature-button" className="light-btn">Read the guide <ArrowUpRight size={16}/></button></div><div className="feature-number">01</div></div><div className="learn-grid">{cards.map(c=><Card className="learn-card" key={c[0]}><div className={`learn-art ${c[4]}`}><span>{c[4]==='mint'?'◒':c[4]==='dark'?'◌':c[4]==='yellow'?'↗':'≈'}</span></div><div className="eyebrow">{c[1]}</div><h3>{c[0]}</h3><p>{c[2]}</p><div className="learn-footer"><span>{c[3]}</span><ArrowUpRight size={16}/></div></Card>)}</div></>}

function Ask(){const [message,setMessage]=useState('');const [answer,setAnswer]=useState('');const [loading,setLoading]=useState(false);const ask=async(q=message)=>{if(!q)return;setMessage(q);setLoading(true);setAnswer('');try{const r=await fetch(`${API}/chat`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:q})});const reader=r.body.getReader();const dec=new TextDecoder();while(true){const {done,value}=await reader.read();if(done)break;setAnswer(a=>a+dec.decode(value));}}catch(e){setAnswer('I’m unable to reach the assistant right now. Your demo data is still available across Finaura.');}setLoading(false)}; return <><div className="page-intro"><div><div className="eyebrow">A second opinion, grounded in your data</div><h2>Ask Finaura<span className="mint">.</span></h2><p>Ask questions in plain language. Get clear, educational answers.</p></div><span className="ai-badge"><Sparkles size={14}/> Context-aware AI</span></div><div className="ask-layout"><Card className="chat-card"><div className="chat-head"><div className="ai-avatar"><Sparkles size={19}/></div><div><strong>Finaura intelligence</strong><small>Knows your demo profile · Always educational</small></div><span className="online"><i></i> Ready</span></div><div className="chat-body"><div className="message assistant"><strong>Ask me about your money, Aarav.</strong><p>I can help you understand your trends, compare months, think through goals, or explain financial concepts.</p></div>{answer&&<div className="message user"><p>{answer}</p></div>}{loading&&<div className="typing"><i></i><i></i><i></i></div>}</div><div className="suggestions">{['Why did my savings decrease?','Which goal should I focus on?','What should I learn next?'].map(q=><button data-testid={`suggested-question-${q.slice(0,8).replaceAll(' ','-').toLowerCase()}`} key={q} onClick={()=>ask(q)}>{q}</button>)}</div><div className="chat-input"><input data-testid="ask-finaura-input" value={message} onChange={e=>setMessage(e.target.value)} onKeyDown={e=>e.key==='Enter'&&ask()} placeholder="Ask anything about your financial life…"/><button data-testid="ask-finaura-send-button" onClick={()=>ask()}><Send size={17}/></button></div><small className="disclaimer">Finaura provides education, not investment orders. This conversation uses demo data.</small></Card><div className="ask-side"><div className="eyebrow">Try asking</div><h3>Make your money clearer.</h3>{['Which month did my expenses increase the most?','What are my highest spending categories?','Why is my financial health score 78?'].map(q=><button data-testid={`ask-prompt-${q.slice(0,10).replaceAll(' ','-').toLowerCase()}`} className="prompt" onClick={()=>ask(q)} key={q}>{q}<ChevronRight size={15}/></button>)}</div></div></>}

function Settings(){const [deleted,setDeleted]=useState(false);return <><div className="page-intro"><div><div className="eyebrow">Trust is a feature</div><h2>Settings & privacy</h2><p>Your financial data belongs to you.</p></div></div><div className="settings-grid"><Card><SectionTitle eyebrow="Privacy principles" title="Built with care"/>{[['Encrypted in transit','Your data travels over HTTPS/TLS.'],['Encrypted at rest','Prototype storage is isolated to your demo profile.'],['No bank credentials','Finaura never asks for or stores bank passwords.'],['Delete anytime','You can remove your financial information below.']].map(([a,b])=><div className="privacy-row" key={a}><div className="privacy-icon"><LockKeyhole size={16}/></div><div><strong>{a}</strong><p>{b}</p></div><Check size={16} className="check"/></div>)}</Card><Card className="danger-card"><div className="eyebrow">Data controls</div><h3>Demo data & prototype status</h3><p>This workspace contains fictional Demo Data for Aarav Sharma. Security features shown here are prototype implementations, not a production audit or certification.</p><button data-testid="delete-data-button" className="danger-btn" onClick={async()=>{await axios.delete(`${API}/financial/data`);setDeleted(true)}}><Trash2 size={16}/> Delete my financial data</button>{deleted&&<div className="deleted-message" data-testid="data-deleted-message">Your saved demo goals and transactions have been deleted.</div>}</Card></div></>}

function AppContent(){const [data,setData]=useState(null);useEffect(()=>{axios.get(`${API}/financial/overview`).then(r=>setData(r.data)).catch(()=>setData(null))},[]);const path=useLocation().pathname;if(!data)return <div className="loading-screen"><div className="brand"><span className="brand-mark">f</span><span>finaura</span></div><p>Building your financial picture…</p></div>;const pages={'/':<Dashboard data={data}/>,'/finances':<Finances data={data}/>,'/statements':<Statements data={data}/>,'/analysis':<Analysis data={data}/>,'/goals':<Goals data={data}/>,'/changes':<Changes data={data}/>,'/learn':<Learn/>,'/ask':<Ask/>,'/settings':<Settings/>};return <Shell>{pages[path]||pages['/']}</Shell>}
export default function App(){return <BrowserRouter><AppContent/></BrowserRouter>}